# Lumina 轻量化 XMP 交付优化方案

> 核心结论：Lumina 不做在线 Lightroom，也不承诺在网页端复刻真实成片。Lumina 的正式交付物是可导入 Lightroom 的 XMP 调色预设；网页端预览只表达风格方向，诊断报告负责解释参数依据。

---

## 1. 产品定位

Lumina 应定位为：

> AI 驱动的 Lightroom 调色方案生成器。

它解决的不是“在网页里完成最终修图”，而是：

- 读取照片内容与基础影像数据；
- 理解用户想要的风格方向；
- 生成一套 Lightroom 可识别、可导入、可继续微调的 XMP 预设；
- 用诊断报告解释为什么这样调；
- 用轻量预览帮助用户判断大致方向。

因此，产品边界必须清晰：

| 模块 | 定位 | 是否作为最终效果承诺 |
|---|---|---|
| XMP 预设 | 正式交付物 | 是 |
| AI 诊断报告 | 参数解释层 | 是，解释逻辑需要可信 |
| 网页效果预览 | 风格方向参考 | 否 |
| Lightroom 渲染结果 | 用户最终使用效果 | 是，但由 Lightroom 完成 |

---

## 2. 为什么不做重型网页调色渲染

### 2.1 色彩适配是长期维护坑

如果 Lumina 在网页端承诺“最终调色效果”，就必须持续维护：

- Canon / Nikon / Sony / Fujifilm / Panasonic / Leica 等厂商色彩差异；
- 同厂不同机型的传感器与色彩响应差异；
- RAW 解码链差异，例如 LibRaw 与 Adobe Camera Raw 的起点不同；
- sRGB / Display P3 / Adobe RGB / ICC profile 的显示差异；
- 浏览器、系统、显示器之间的色彩管理差异；
- Lightroom 私有 profile、process version、tone mapping 与 HSL 解释差异；
- Adobe / 浏览器 / 新相机格式更新带来的持续兼容问题。

这些问题不是一次性开发成本，而是持续维护成本。

### 2.2 网页端复刻 Lightroom 性价比低

完整复刻 Lightroom 需要处理：

- RAW demosaic；
- camera profile；
- white balance；
- tone curve；
- HSL / Color Mixer；
- Color Grading；
- lens correction；
- noise reduction；
- sharpening；
- local adjustment；
- process version compatibility；
- XMP 参数到渲染管线的真实映射。

这会把项目从“AI 调色预设生成器”变成“在线图像编辑器”，复杂度和当前产品目标不匹配。

### 2.3 预览越真实，误解风险越高

如果网页预览看起来像最终成片，用户会天然期待：

> 网页里看到什么，Lightroom 导入后就是什么。

但由于 Lightroom 的真实渲染链不可完全复刻，这个承诺很容易被打破。更稳妥的策略是从产品文案和 UI 上明确：

> 预览用于展示调色方向，最终效果以 Lightroom 导入 XMP 后为准。

---

## 3. 核心方案

### 3.1 正式交付：XMP 调色预设

XMP 是 Lumina 的核心产物。

要求：

- 参数必须符合 Lightroom 可识别字段；
- 参数必须经过范围钳制，避免生成异常预设；
- 文件命名清晰，包含品牌、日期、风格或场景；
- 下载链路稳定；
- 用户能直接导入 Lightroom Classic / Lightroom CC；
- 多轮微调后能重新生成新的 XMP。

建议命名：

```text
Lumina_Portrait_Warm_20260508.xmp
Lumina_Cinematic_Cool_20260508.xmp
Lumina_Film_Soft_20260508.xmp
```

### 3.2 解释交付：AI 调色诊断报告

诊断报告是建立信任的关键。用户不能只看到一组参数，还要知道参数为什么合理。

报告建议分为四层：

1. 画面诊断：说明照片主体、光线、曝光、色彩倾向；
2. 底片/图像数据分析：说明高光、暗部、ISO、直方图、色彩风险；
3. 调色策略：说明整体风格方向；
4. 核心参数动作：说明具体参数变化及原因。

示例：

```text
【Highlights2012】-38：压回天空与皮肤高光，避免亮部发白。
【Shadows2012】+22：在暗部存活率允许范围内提亮阴影。
【Temperature】5200K：略微降温，建立清爽冷调基础。
【Vibrance】+16：提升环境色彩，同时避免肤色过饱和。
```

### 3.3 辅助交付：轻量效果方向预览

预览不追求还原 Lightroom，只做低成本方向表达。

推荐使用：

- CSS filter；
- Canvas 2D；
- 少量 overlay；
- split view；
- before / after 对比。

不推荐在近期做：

- 完整 RAW 渲染；
- 厂商 profile 适配；
- Lightroom 私有渲染模拟；
- WebGL/WebGPU 重型调色引擎；
- AI 生成式最终成片。

预览文案建议：

```text
效果方向预览
根据当前 XMP 参数进行近似模拟，实际效果会因相机配置文件、Lightroom 渲染版本与显示设备略有差异。
```

---

## 4. 功能分层策略

### Level 1：当前优先级，轻量预览

目标：让用户直观看到调色方向。

实现：

- Exposure → brightness；
- Contrast / Clarity / Dehaze → contrast；
- Vibrance / Saturation → saturate；
- Temperature / Tint → overlay 色温和色偏；
- Vignette → radial-gradient；
- Split view 对比原图与模拟效果。

优点：

- 开发成本低；
- 性能稳定；
- 与当前架构兼容；
- 不引入厂商色彩维护。

限制：

- 不等同 Lightroom；
- HSL、曲线、降噪、锐化只能近似表达；
- 不适合作为最终效果承诺。

### Level 2：可选增强，Canvas 参数模拟

目标：让预览比 CSS filter 更细腻，但仍不追求真实 Lightroom。

可以做：

- 像素级曝光、对比、饱和度；
- 简化 tone curve；
- 简化 HSL；
- 简化 color grading；
- 导出一张“参考预览图”。

不做：

- RAW demosaic；
- camera profile；
- 厂商机型适配；
- Adobe process version 还原。

### Level 3：暂不建议，WebGL/LUT 预览

适合未来产品验证后再考虑。

可以提供：

- `.cube` LUT 导出；
- WebGL shader 预览；
- 视频/影像软件用户扩展。

风险：

- 研发复杂度明显上升；
- 色彩一致性仍然无法和 Lightroom 完全对齐；
- 容易偏离 XMP 核心交付。

### Level 4：不建议近期投入，完整 Lightroom-like 渲染

不建议原因：

- 成本极高；
- 维护周期长；
- 难以保证真实性；
- 会改变产品性质；
- 与当前 MVP 目标不匹配。

---

## 5. 当前项目优化重点

### 5.1 固定产品叙事

所有页面、按钮、提示文案围绕这句话：

> Lumina 不替代 Lightroom，Lumina 负责生成 Lightroom 可以理解的专业调色方案。

建议替换用词：

| 不建议 | 建议 |
|---|---|
| 最终效果图 | 效果方向预览 |
| AI 修好后的图片 | XMP 参数模拟预览 |
| 在线完成调色 | 生成 Lightroom 调色预设 |
| 真实还原 Lightroom | 近似展示风格方向 |
| 一键生成成片 | 一键生成可导入预设 |

### 5.2 强化 XMP 质量

优先级最高。

要做：

- 明确所有 Lightroom 参数白名单；
- 对每个参数做合法范围钳制；
- 对未知字段做保守处理；
- ToneCurve 数组必须校验；
- XMP 模板保持稳定；
- 生成后可做 XML 结构校验；
- 增加导入说明；
- 生成文件名包含风格信息。

验收标准：

- 导入 Lightroom 不报错；
- 默认参数不污染照片；
- 微调后重新生成的 XMP 下载正常；
- 不同风格之间有明显但不过度的差异；
- 参数说明能对应报告里的调色理由。

### 5.3 强化诊断报告可信度

报告比预览更重要。

要做：

- 区分 RAW 高可信与 JPG 参考级；
- 明确提示 JPG 不是 RAW 物理宽容度；
- 对高光溢出、暗部死黑、ISO 噪声给出具体风险；
- 每条核心参数动作必须说明“为什么”；
- 避免空泛审美词堆砌；
- 避免“高级感、电影感、通透感”这类无参数依据的泛化表达。

建议增加评分/置信度：

| 场景 | 标签 |
|---|---|
| RAW + 完整数据 | RAW 高可信 |
| JPG + EXIF 完整 | JPG 参考级 |
| 缺失 EXIF | 数据不足，保守建议 |
| 高 ISO / 高压缩 | 画质风险较高 |
| 高光溢出严重 | 高光恢复空间有限 |

### 5.4 明确预览边界

预览层只做三件事：

1. 展示风格方向；
2. 支持原图 / 模拟 / 分屏对比；
3. 帮助用户决定是否下载或微调 XMP。

预览层不做：

- 不承诺最终 Lightroom 效果；
- 不承诺厂商色彩一致；
- 不承诺 RAW 真实显影；
- 不做大规模机型适配；
- 不做在线精修。

UI 中建议加入轻量说明：

```text
近似预览
此处仅模拟当前 XMP 参数的视觉方向，最终效果以 Lightroom 导入后为准。
```

### 5.5 右侧 Chat 改造成“调色助手”

右侧输入区不建议定位为普通 Chat，也不建议让用户和 AI 开放式闲聊。它更适合定位为：

> 自然语言调参面板 + XMP 版本控制器。

也就是说，用户不是在和 AI 聊天，而是在用自然语言控制当前 Lightroom 预设。每一次输入都应该产生一版新的调色方案，并且能被追踪、对比、撤回和重新下载。

推荐命名：

- 调色助手；
- 微调方案；
- 修改方案；
- 参数指令；
- Refine。

最推荐使用：

```text
调色助手
```

因为“Chat”会让用户期待泛聊天；“调色助手”会让用户理解这里是用来修改当前 XMP 调色方案的。

#### 5.5.1 交互原则

调色助手只做四件事：

1. 接收用户的自然语言微调指令；
2. 基于当前版本生成新的 XMP 参数；
3. 展示本轮参数变化和原因；
4. 管理历史版本、撤回和下载。

它不做：

- 不回答泛泛的摄影知识问答；
- 不作为开放式聊天机器人；
- 不生成最终成片；
- 不绕过 XMP 参数体系；
- 不把 AI 回复文本作为主要价值。

#### 5.5.2 推荐信息架构

右侧仍保留主内容 tab：

```text
诊断报告 | 参数调节
```

底部固定一个调色助手区域：

```text
调色助手
当前版本：V2

[肤色更亮] [高光再压一点] [冷调更明显] [胶片颗粒弱一点]

最近调整：
肤色再亮一点
Exposure +0.15 / Orange Luminance +12 / Highlights -6
[查看变化] [撤回上一版] [下载此版 XMP]

输入框：告诉我你想怎么微调这套预设...
```

这样它会更像专业工具里的“自然语言调参历史面板”，而不是普通聊天窗口。

#### 5.5.3 每轮微调都生成版本

首次分析完成后生成 `V1`。之后每次 refine 生成一个新版本：

```text
V1 初始方案
V2 肤色再亮一点
V3 胶片颗粒弱一点
V4 整体冷一点但保留肤色
```

每个版本都应保存：

- 用户指令；
- AI 改动摘要；
- 修改前参数；
- 修改后参数；
- changed params；
- 对应诊断报告；
- 对应下载链接；
- 创建时间。

推荐前端数据结构：

```ts
type RefineVersion = {
  id: string
  parentId?: string
  userIntent: string
  assistantSummary: string
  paramsBefore: Record<string, number>
  paramsAfter: Record<string, number>
  changedParams: {
    key: string
    before: number
    after: number
    delta: number
    reason?: string
  }[]
  report: DiagnosticReport
  downloadUrl: string
  createdAt: string
}
```

#### 5.5.4 参数变化必须可见

调色助手的核心不是 AI 回复，而是参数 diff。

示例：

```text
本轮调整
Exposure2012              0.20 → 0.35
Highlights2012           -28   → -36
LuminanceAdjustmentOrange 10    → 22
Temperature              5500  → 5200
```

更适合 UI 的轻量写法：

```text
+0.15 Exposure
-8 Highlights
+12 Orange Luminance
-300K Temperature
```

这能让用户明确知道“AI 到底改了什么”。相比一段很长的 AI 回复，参数变化更专业、更可信，也更符合 XMP 交付定位。

#### 5.5.5 快捷指令应上下文生成

当前固定 chip 可以保留，但更好的方案是根据诊断结果动态生成。

人像场景：

```text
肤色更干净
背景再冷一点
降低脸部高光
胶片感弱一点
```

风景场景：

```text
天空更蓝
高光再压一点
暗部更通透
整体更电影感
```

JPG 高风险场景：

```text
保守一点
降低饱和度
减少颗粒
避免高光过曝
```

这样用户会感觉助手理解当前照片，而不是提供一组通用按钮。

#### 5.5.6 后端返回建议

当前 `/api/refine` 可以继续返回 `diagnostic_report`、`lightroom_params`、`download_url`，但建议扩展 `assistant_summary` 和 `changed_params`：

```json
{
  "type": "final",
  "session_id": "sess_xxx",
  "assistant_summary": "已提升肤色明度，同时轻微压低高光，避免脸部发白。",
  "changed_params": [
    {
      "key": "Exposure2012",
      "before": 0.2,
      "after": 0.35,
      "reason": "整体肤色略暗，轻微提亮中间调"
    },
    {
      "key": "LuminanceAdjustmentOrange",
      "before": 10,
      "after": 22,
      "reason": "提高肤色明度"
    }
  ],
  "diagnostic_report": {},
  "lightroom_params": {},
  "download_url": "/downloads/xxx.xmp"
}
```

如果后端暂时不返回 `changed_params`，前端可以先用 `paramsBefore` 和 `paramsAfter` 自己 diff。

#### 5.5.7 最小可行版本

第一版不需要复杂，只需要做到：

- [ ] 把 Chat 命名改为“调色助手”；
- [ ] refine 后显示一句本轮改动摘要；
- [ ] 前端 diff 出 changed params；
- [ ] 支持撤回上一版；
- [ ] 保留最近 3 次微调历史；
- [ ] 每个版本都能下载对应 XMP。

#### 5.5.8 进阶版本

后续可以增加：

- 参数滑块旁高亮本轮修改；
- V1 / V2 / V3 版本对比；
- 一键恢复任意版本；
- 每个版本单独下载 XMP；
- AI 自动推荐下一步微调；
- 风格强度：弱 / 中 / 强；
- 将自然语言解析成结构化 intent，例如：

```ts
type RefineIntent = {
  target: "skin" | "highlight" | "shadow" | "color" | "grain" | "global"
  direction: "brighter" | "darker" | "warmer" | "cooler" | "stronger" | "softer"
  strength: number
}
```

#### 5.5.9 推荐输入文案

Placeholder：

```text
告诉我你想怎么微调这套预设...
```

快捷指令：

```text
肤色更亮
高光再压一点
冷调更明显
胶片颗粒弱一点
暗部别太灰
```

避免使用：

```text
输入你想问的问题...
帮我看看这张图怎么样
你觉得好看吗
```

这类问题应交给诊断报告，而不是调色助手。

---

## 6. 技术路线建议

### 6.1 前端

优先处理：

- 保持 Worker 解析 RAW，不阻塞主线程；
- JPG 本地分析只提取轻量直方图、EXIF、色彩空间；
- 预览维持 CSS filter / Canvas 轻量模拟；
- 对 NEF 预览图明确说明是内嵌预览，不是完整 RAW 渲染；
- 分屏对比和缩放交互优先于复杂渲染。
- 调色助手维护本地版本历史，至少支持最近 3 版；
- refine 完成后前端 diff 参数，展示本轮变化；
- 参数滑块对本轮修改项做轻量高亮。

不优先处理：

- 完整 RAW viewer；
- 厂商 profile；
- WebGL 调色器；
- 在线导出最终调色图片。

### 6.2 后端

优先处理：

- XMP 生成稳定性；
- LLM 输出解析防御；
- 参数钳制；
- Session 微调链路；
- SSE 断开取消；
- 本地规则兜底；
- 下载文件 TTL 清理。
- `/api/refine` 可选返回 `assistant_summary` 与 `changed_params`；
- 每轮微调都能生成独立 XMP 下载链接。

不优先处理：

- 后端图像渲染；
- 图像转码服务；
- 大文件 RAW 上传；
- 多厂商色彩 profile 服务。

### 6.3 AI 调用

首轮分析：

- 传预览图；
- 传轻量物理/像素数据；
- 输出诊断报告和参数 JSON。

多轮微调：

- 不重传图片；
- 使用 session 中的上一轮参数和图像摘要；
- 输出新的诊断报告和参数 JSON；
- 输出本轮修改摘要；
- 输出关键参数变化理由。

要求：

- 输出必须是结构化 JSON；
- 参数必须能被程序解析；
- 报告和参数必须互相对应；
- 不允许只给审美描述而不给参数依据。
- 不允许把微调回复写成泛聊天；必须落到参数变化。

---

## 7. 近期执行计划

### P0：先收口定位与稳定性

- [ ] 全站文案统一：XMP 是正式交付，预览是方向参考；
- [ ] 预览区域增加“近似预览”说明；
- [ ] 下载按钮文案改为“下载 Lightroom XMP 预设”；
- [ ] 诊断报告里加入 RAW/JPG 可信度标签；
- [ ] 修正默认 dev 启动方式，确保本地一条命令可运行；
- [ ] 清理仓库中的 `._*`、`.next`、临时产物和未跟踪噪音；
- [ ] 让后端测试依赖进入 lockfile，确保 `pnpm test` 可运行。

### P1：提升 XMP 专业度

- [ ] 建立 Lightroom 参数白名单；
- [ ] 增加 XMP 生成测试；
- [ ] 增加 ToneCurve 校验；
- [ ] 文件名加入风格标签；
- [ ] 对导入 Lightroom 的步骤做简短引导；
- [ ] 多轮微调后保留上一版参数对比；
- [ ] 调色助手每次 refine 生成一个版本；
- [ ] 每个微调版本保留独立下载链接。

### P2：提升用户感知

- [ ] 增加原图 / 预览 / 分屏三模式；
- [ ] 增加“调色方向置信度”；
- [ ] 增加“为什么这样调”的核心动作卡片；
- [ ] 增加一键复制参数摘要；
- [ ] 增加历史版本和 Undo；
- [ ] 增加不同风格 preset 对比。
- [ ] 快捷微调 chip 根据场景动态生成；
- [ ] 参数滑块高亮本轮修改项；
- [ ] 增加 V1 / V2 / V3 版本对比视图。

### P3：未来探索

- [ ] Canvas 2D 更细的参数预览；
- [ ] `.cube` LUT 导出；
- [ ] 批量照片套同一风格；
- [ ] Lightroom 导入指引视频或图文；
- [ ] Adobe OAuth / 云同步可行性调研。

---

## 8. 风险与边界

| 风险 | 应对 |
|---|---|
| 用户误以为网页预览就是最终效果 | 所有 UI 文案明确“近似预览，最终以 Lightroom 为准” |
| 不同相机导入后效果差异明显 | 不承诺网页复刻，只交付 XMP 方案；报告中提示 RAW/JPG 可信度 |
| AI 参数过激 | 参数白名单 + 范围钳制 + 风格强度控制 |
| XMP 导入失败 | 模板测试 + 下载文件校验 + 导入说明 |
| 预览效果不够惊艳 | 用报告和参数解释建立专业感，不靠网页渲染硬撑 |
| 项目膨胀成在线修图工具 | 坚持“不替代 Lightroom”的产品原则 |
| 用户把调色助手当成通用聊天机器人 | 命名为“调色助手”，输入框和快捷指令全部围绕微调 XMP |
| 用户不知道每次微调改了什么 | 展示 changed params、改动摘要和版本历史 |

---

## 9. 推荐页面文案

### 主标语

```text
AI 生成 Lightroom 调色预设
```

### 副标题

```text
上传照片，Lumina 会分析画面与影像数据，生成可直接导入 Lightroom 的 XMP 调色方案。
```

### 预览区说明

```text
效果方向预览
此处根据 XMP 参数进行近似模拟，实际效果会因相机配置文件、Lightroom 渲染版本与显示设备略有差异。
```

### 下载按钮

```text
下载 Lightroom XMP 预设
```

### 报告区标题

```text
AI 调色诊断
```

### 风险提示

```text
JPG 文件已包含相机处理与压缩结果，分析结论为参考级；RAW 文件在可读取底层数据时可信度更高。
```

### 调色助手标题

```text
调色助手
```

### 调色助手说明

```text
用自然语言微调当前 XMP 预设，每次调整都会生成一个可回退、可下载的新版本。
```

### 调色助手输入框

```text
告诉我你想怎么微调这套预设...
```

### 本轮调整摘要

```text
本轮调整
Exposure +0.15 / Highlights -8 / Orange Luminance +12
```

---

## 10. 最终原则

Lumina 的长期产品原则：

> 不复刻 Lightroom，不维护厂商色彩渲染，不承诺网页端最终成片。  
> 专注生成专业、可解释、可导入、可微调的 Lightroom XMP 调色方案。

右侧调色助手的长期原则：

> 不做泛聊天，不做最终修图，不替代参数面板。  
> 用自然语言生成可解释、可对比、可回退、可下载的 XMP 版本。

这条原则可以帮助项目持续保持轻量：

- 技术上避免陷入色彩引擎维护；
- 产品上避免用户误解；
- 体验上保留直观预览；
- 商业上聚焦“专业调色方案生成”这个差异化价值。
